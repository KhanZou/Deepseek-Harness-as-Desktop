// Host entry for the live-stats plugin. All behavior lives in the browser
// half (lib/client.js): it registers a per-turn stats line via the
// conversation.chat.turnTail chain.
export function apply() {}