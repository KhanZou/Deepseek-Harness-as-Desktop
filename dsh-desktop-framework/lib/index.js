// Host entry for the dsh-desktop-framework plugin. All behavior lives in the
// browser half (lib/client.js): settings framework, panels framework and the
// Files/Changes/Terminal panel tabs.
export function apply() {}
