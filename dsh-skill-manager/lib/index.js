// Host entry for the dsh-skill-manager plugin. All behavior lives in the
// browser half (lib/client.js), which fills ui-workspace's
// `sidebar.workspaces.skillManager` hole and drives the skill.* manager RPCs.
//
// Requires the dsh host-side skill-config support that is NOT part of the
// stock release yet (see ../../docs/skill-manager-host-support.md).
export function apply() {}
